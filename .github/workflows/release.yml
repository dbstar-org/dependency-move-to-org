name: release

on: workflow_dispatch

jobs:
  release:
    permissions:
      contents: write
      packages: write
    uses: dbstarll/general/.github/workflows/maven-release.yml@v1
    secrets: inherit