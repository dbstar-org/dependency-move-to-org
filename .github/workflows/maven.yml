name: maven

on:
  push:
    branches:
      - main
      - snapshot-*
    paths-ignore:
      - '.github/workflows/release.yml'
      - 'README.md'
  workflow_dispatch:

env:
  top-env: abc

jobs:
  maven:
    permissions:
      packages: write
    uses: dbstar-org/general/.github/workflows/maven.yml@main
    secrets: inherit